/// Textos de error que la app usa cuando el backend no envía uno más específico.
class ApiConfigMessages {
  const ApiConfigMessages._();

  static const String invalidData = 'Revisa los datos del formulario e inténtalo de nuevo.';
}
